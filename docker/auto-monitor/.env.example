# Uptime Kuma Configuration
# Note: API keys are not supported for monitor management via Socket.io API
# You must use username and password authentication
# When running in Docker container: use http://uptime-kuma:3001
# When running locally: use your external URL (e.g., https://uptime.example.com)
UPTIME_KUMA_URL=http://uptime-kuma:3001
UPTIME_KUMA_USERNAME=admin
UPTIME_KUMA_PASSWORD=your_password_here

# Traefik Servers
# You can configure multiple Traefik servers by incrementing the number
# Each server should have a URL and a GROUP (group is used for tagging monitors)

TRAEFIK_1_URL=http://localhost:8080
TRAEFIK_1_GROUP=Local Server

# Example: Add more Traefik servers
# TRAEFIK_2_URL=http://traefik.example.com:8080
# TRAEFIK_2_GROUP=Production Server

# TRAEFIK_3_URL=http://traefik-staging.example.com:8080
# TRAEFIK_3_GROUP=Staging Server

# Docker Servers
# You can configure multiple Docker servers by incrementing the number
# Each server should have a URL and a GROUP (group is used for tagging monitors)
# The Docker host in Uptime Kuma will be named: {GROUP}-docker
# URL format: tcp://host:port or http://host:port

DOCKER_1_URL=tcp://docker-proxy-local:2375
DOCKER_1_GROUP=Local

# Example: Add more Docker servers (from WireGuard VPN)
# DOCKER_2_URL=tcp://10.200.200.2:2375
# DOCKER_2_GROUP=hal-wg

# DOCKER_3_URL=tcp://10.200.200.3:2375
# DOCKER_3_GROUP=plague-wg

# DOCKER_4_URL=tcp://10.200.200.4:2375
# DOCKER_4_GROUP=blade-wg

# Ignore Patterns (comma-separated, supports wildcards)
# Hosts matching these patterns won't have monitors created
# Existing monitors matching these patterns will be removed
# Examples:
#   *redis*         - matches any host containing "redis"
#   *traefik*       - matches any host containing "traefik"
#   test.example.com - matches exact hostname
#   *.internal.com  - matches any subdomain of internal.com
IGNORE_PATTERNS=*redis*,*traefik*

# Reset Tags (true/false)
# When enabled, removes ALL existing tags from monitors and adds only the correct group tag
# Use this to clean up monitors that have accumulated incorrect or old tags
# After running once with this enabled, set it back to false
RESET_TAGS=false
